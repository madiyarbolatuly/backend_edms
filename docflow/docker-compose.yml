qversion: "3"
services:
  api:
    depends_on:
      - postgres
    ports:
      - "8000:8000"
    env_file: app/.env
q    image: postgres
    env_file: app/.env
    volumes:
      - postgres-db:/var/lib/postgresql/data
      - ./migrations:/usr/src/app/migrations

volumes:
  postgres-db: