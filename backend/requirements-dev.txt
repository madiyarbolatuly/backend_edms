# Test-only dependencies. Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt
#   pytest
#
# The suite runs against SQLite in memory, so no Postgres and no running
# services are needed — see tests/conftest.py for why that is sound here.
pytest>=8.0
pytest-asyncio>=0.24
aiosqlite>=0.20
# SQLAlchemy's asyncio support needs greenlet; it is not always pulled in on
# every platform/wheel combination.
greenlet>=3.0
